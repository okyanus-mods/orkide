{
  "schemaVersion": 1,
  "id": "orkide",
  "version": "${version}",
  "name": "Orkide",
  "description": "BukkitAPI in Fabric via Okyanus",
  "environment": "server",
  "depends": {
    "fabricloader": ">=0.4.0",
    "okyanus": ">=0.1.0"
  },
  "entrypoints": {
    "server": [
      "club.issizler.orkide.OrkideMod::init"
    ]
  }
}
